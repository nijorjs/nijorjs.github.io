<template specs={title,content,link}>
    <div class="card">
        <div class="title">{title}</div>
        <div>{content}</div>
    </div>
</template>
<style>
    .card{
        background-color: white;
    }
</style>